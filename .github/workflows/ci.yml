name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore Epubra.sln

      - name: Build
        run: dotnet build Epubra.sln -c Release --no-restore

      - name: Test
        run: dotnet test tests/Epubra.Tests/Epubra.Tests.csproj -c Release --no-build --verbosity normal
