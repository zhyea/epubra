# 移除「章节」Ribbon Tab — 完成报告

## 决策
用户确认：**完全移除**工具栏中的「章节」Tab（P11.1 曾将其从 3 组扩到 6 组）。

## 可行性分析结论
- 章节 Tab 的 **14 个命令**，在 TreeView 右键菜单中 **14/14 全覆盖**
- 添加章节 / 删除 / 上移 / 下移 在左侧章节树**面板头**仍有按钮；重命名有 **F2** 快捷键
- 代码后台仅引用 `RibbonTabs` 控件本身（P8 折叠逻辑），无依赖章节 Tab 的切换代码 → **移除零逻辑风险**
- 唯一代价：功能**可发现性**下降（新用户需摸索右键菜单）

## 执行改动
- 删除 `src/Epubra.App/Views/MainWindow.xaml` 中 `<TabItem Header="章节">` 整块（约 66 行，单处 XAML 删除）
- Ribbon Tab 由 5 个（开始 / 导入 / 插入 / 章节 / 发布）变为 **4 个**

## 验证结果
- 构建：**0 错误**（仅既有 WebView2 版本警告 NU1603）
- 测试：**120/120 全绿，0 失败**，无回归

## 构建环境备注
- 首次构建遇到 `MSB3021/3027` 文件复制锁，源于一个**运行中的 Epubra.exe 实例**（PID 12348）持锁 bin 目录；XAML 编译本身已通过（非代码错误）。
- 通过 PowerShell `Stop-Process -Force` 释放锁后，构建与测试均正常。
- 提示：若从 bin 目录直接启动 Epubra.exe 做自测，再构建会被自身进程锁住，需先关闭该实例。

## 命令入口去向（移除后）
| 原章节 Tab 命令 | 新入口 |
|---|---|
| 添加章节 / 添加子章节 / 重命名(F2) / 删除 | 右键菜单 + 面板头按钮 |
| 上移 / 下移 | 右键菜单 + 面板头按钮 |
| 升级 / 降级 | 右键菜单 |
| 合并 / 手动拆分 / 自动切分 | 右键菜单 |
| 首行作目录名 / 批量重命名 | 右键菜单 |
| 一键排版 | 右键菜单 |
| 缩进 / 取消缩进 | 面板头按钮（原本就有） |
